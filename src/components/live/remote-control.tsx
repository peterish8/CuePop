"use client";
import { useEffect, useMemo, useState } from "react";
import { ChevronLeft, ChevronRight, CircleStop, Eye, Lock, Play, Radio, RotateCcw, Unlock, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { CuePopLogo } from "@/components/logo";
import { toast } from "@/components/ui/toaster";
import { useRoom } from "@/components/live/use-room";
import type { DeckItem } from "@/lib/schema";
import type { HostCommandType } from "@/lib/live/types";

export function RemoteControl({code,token}:{code:string;token:string}){const {snapshot,connected,socket}=useRoom(code);const [items,setItems]=useState<DeckItem[]>([]);const [loading,setLoading]=useState(true);const [accessError,setAccessError]=useState<string|null>(null);useEffect(()=>{if(!token){setAccessError("This remote link is missing its private control token.");setLoading(false);return}fetch(`/api/live/${code}?token=${encodeURIComponent(token)}`).then(r=>r.json()).then(body=>{if(body.ok)setItems(body.data.items);else{setAccessError(body.error||"This remote link is invalid.");toast({title:"Remote link rejected",description:body.error,tone:"error"})}}).catch(()=>setAccessError("The phone remote could not be loaded.")).finally(()=>setLoading(false))},[code,token]);const current=useMemo(()=>{if(!snapshot?.currentItem)return null;const found=items.find(i=>i.id===snapshot.currentItem?.id);return found||{...snapshot.currentItem,notes:null}},[snapshot?.currentItem,items]);function command(command:HostCommandType){if(!socket){toast({title:"Realtime connection unavailable",tone:"error"});return}socket.emit("host:command",{code,token,command},(ack:any)=>{if(!ack?.ok)toast({title:"Command rejected",description:ack?.error,tone:"error"})})}if(loading)return <main className="grid min-h-svh place-items-center bg-[#07090c] text-[#7f8993]">Connecting remote…</main>;if(accessError)return <main className="grid min-h-svh place-items-center bg-[#07090c] p-6 text-center text-white"><div className="max-w-sm"><Lock className="mx-auto size-8 text-[var(--warning)]"/><h1 className="mt-5 text-2xl font-semibold">Remote access denied</h1><p className="mt-3 text-sm leading-6 text-[#84909a]">{accessError}</p></div></main>;if(!snapshot)return <main className="grid min-h-svh place-items-center bg-[#07090c] text-[#7f8993]">Waiting for the live room…</main>;const moment=current&&current.type!=="slide";return <main className="min-h-svh bg-[#07090c] px-5 py-6 text-white"><div className="mx-auto max-w-md"><div className="flex items-center justify-between"><CuePopLogo/><div className="flex items-center gap-2 text-xs text-[#77828c]"><span className={`size-2 rounded-full ${connected?"bg-[var(--success)]":"bg-[var(--warning)]"}`}/>{code}</div></div><div className="mt-8 rounded-3xl border border-white/[.08] bg-[#0c1014] p-5"><div className="flex items-start justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-[.14em] text-[var(--cyan)]">Now showing</div><h1 className="mt-3 text-2xl font-semibold leading-tight tracking-[-.04em]">{snapshot.status==="join"?"Dynamic join screen":current?.title||"No item"}</h1></div><span className="rounded-full border border-white/[.08] px-2.5 py-1 text-[10px] text-[#78838d]">{snapshot.status}</span></div>{current?.notes&&<p className="mt-4 rounded-2xl border border-white/[.065] bg-white/[.025] p-4 text-sm leading-6 text-[#8a959f]">{current.notes}</p>}<div className="mt-5 flex items-center gap-2 text-sm text-[#8b96a0]"><Users className="size-4 text-[var(--cyan)]"/>{snapshot.attendeeCount} joined {moment&&<>· {snapshot.responseCount} answered</>}</div></div>
 <div className="mt-4 grid grid-cols-2 gap-3"><Button size="lg" variant="secondary" disabled={snapshot.currentIndex<=0||snapshot.status==="join"||snapshot.status==="active"} onClick={()=>command("previous")}><ChevronLeft className="size-5"/>Previous</Button><Button size="lg" variant="secondary" disabled={snapshot.currentIndex>=snapshot.totalItems-1||snapshot.status==="join"||snapshot.status==="active"} onClick={()=>command("next")}>Next<ChevronRight className="size-5"/></Button></div>
 <div className="mt-3 space-y-3">{snapshot.status==="join"?<Button size="lg" className="w-full" variant="accent" onClick={()=>command("start")}><Play className="size-5"/>Start presentation</Button>:moment?<><Button size="lg" className="w-full" variant="accent" disabled={snapshot.status!=="presenting"} onClick={()=>command("open")}><Radio className="size-5"/>Open voting</Button><div className="grid grid-cols-2 gap-3"><Button size="lg" variant="secondary" disabled={snapshot.status!=="active"} onClick={()=>command("close")}><CircleStop className="size-5"/>Close</Button><Button size="lg" variant="secondary" disabled={snapshot.status!=="closed"} onClick={()=>command("reveal")}><Eye className="size-5"/>Reveal</Button></div></>:<Button size="lg" className="w-full" variant="accent" disabled={snapshot.currentIndex>=snapshot.totalItems-1} onClick={()=>command("next")}><ChevronRight className="size-5"/>Advance slide</Button>}</div>
 <div className="mt-8 border-t border-white/[.065] pt-5"><div className="grid grid-cols-2 gap-3"><Button variant="ghost" onClick={()=>command(snapshot.joinLocked?"unlockJoin":"lockJoin")}>{snapshot.joinLocked?<><Unlock className="size-4"/>Unlock join</>:<><Lock className="size-4"/>Lock join</>}</Button><Button variant="ghost" onClick={()=>command("showJoin")}><RotateCcw className="size-4"/>Join screen</Button></div><Button className="mt-3 w-full" variant="danger" onClick={()=>confirm("End the session?")&&command("end")}><CircleStop className="size-4"/>End session</Button></div></div></main>}
