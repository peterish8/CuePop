"use client";
import { useEffect, useMemo, useState } from "react";
import { ChevronLeft, ChevronRight, CircleStop, Eye, Lock, Play, Radio, RotateCcw, Unlock, Users } from "lucide-react";
import { Button } from "@/components/ui/button";
import { CuePopLogo } from "@/components/logo";
import { toast } from "@/components/ui/toaster";
import { useRoom } from "@/components/live/use-room";
import type { DeckItem } from "@/lib/schema";
import type { HostCommandType } from "@/lib/live/types";

export function RemoteControl({code,token}:{code:string;token:string}){const {snapshot,connected,socket}=useRoom(code);const [items,setItems]=useState<DeckItem[]>([]);const [loading,setLoading]=useState(true);const [accessError,setAccessError]=useState<string|null>(null);useEffect(()=>{if(!token){setAccessError("This remote link is missing its private control token.");setLoading(false);return}fetch(`/api/live/${code}?token=${encodeURIComponent(token)}`).then(r=>r.json()).then(body=>{if(body.ok)setItems(body.data.items);else{setAccessError(body.error||"This remote link is invalid.");toast({title:"Remote link rejected",description:body.error,tone:"error"})}}).catch(()=>setAccessError("The phone remote could not be loaded.")).finally(()=>setLoading(false))},[code,token]);const current=useMemo(()=>{if(!snapshot?.currentItem)return null;const found=items.find(i=>i.id===snapshot.currentItem?.id);return found||{...snapshot.currentItem,notes:null}},[snapshot?.currentItem,items]);function command(command:HostCommandType){if(!socket){toast({title:"Realtime connection unavailable",tone:"error"});return}socket.emit("host:command",{code,token,command},(ack:any)=>{if(!ack?.ok)toast({title:"Command rejected",description:ack?.error,tone:"error"})})}if(loading)return <main className="grid min-h-svh place-items-center bg-[var(--color-surface)] text-[var(--color-foreground-subtle)]">Connecting remote…</main>;if(accessError)return <main className="grid min-h-svh place-items-center bg-[var(--color-surface)] p-6 text-center text-white"><div className="max-w-sm"><Lock className="mx-auto size-8 text-[var(--color-warning)]"/><h1 className="mt-5 text-2xl font-semibold">Remote access denied</h1><p className="mt-3 text-sm leading-6 text-[var(--color-foreground-muted)]">{accessError}</p></div></main>;if(!snapshot)return <main className="grid min-h-svh place-items-center bg-[var(--color-surface)] text-[var(--color-foreground-subtle)]">Waiting for the live room…</main>;const moment=current&&current.type!=="slide";return <main className="min-h-svh bg-[var(--color-surface)] px-5 py-6 text-white"><div className="mx-auto max-w-md"><div className="flex items-center justify-between"><CuePopLogo/><div className="flex items-center gap-2 text-xs text-[var(--color-foreground-subtle)]"><span className={`size-2 rounded-full ${connected?"bg-[var(--color-success)]":"bg-[var(--color-warning)]"}`}/>{code}</div></div><div className="mt-8 rounded-3xl border border-[var(--color-border)] bg-[var(--color-surface-elevated)] p-5"><div className="flex items-start justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-[.14em] text-[var(--cyan-strong)]">Now showing</div><h1 className="mt-3 text-2xl font-semibold leading-tight tracking-[-.04em]">{snapshot.status==="join"?"Dynamic join screen":current?.title||"No item"}</h1></div><span className="rounded-full border border-[var(--color-border)] px-2.5 py-1 text-[10px] text-[var(--color-foreground-subtle)]">{snapshot.status}</span></div>{current?.notes&&<p className="mt-4 rounded-2xl border border-[var(--color-border)] bg-white/[.025] p-4 text-sm leading-6 text-[var(--color-foreground-muted)]">{current.notes}</p>}<div className="mt-5 flex items-center gap-2 text-sm text-[var(--color-foreground-muted)]"><Users className="size-4 text-[var(--cyan-strong)]"/>{snapshot.attendeeCount} joined {moment&&<>· {snapshot.responseCount} answered</>}</div></div>
 <div className="mt-4 grid grid-cols-2 gap-3"><Button size="lg" variant="secondary" disabled={snapshot.currentIndex<=0||snapshot.status==="join"||snapshot.status==="active"} onClick={()=>command("previous")}><ChevronLeft className="size-5"/>Previous</Button><Button size="lg" variant="secondary" disabled={snapshot.currentIndex>=snapshot.totalItems-1||snapshot.status==="join"||snapshot.status==="active"} onClick={()=>command("next")}>Next<ChevronRight className="size-5"/></Button></div>
 <div className="mt-3 space-y-3">{snapshot.status==="join"?<Button size="lg" className="w-full" variant="primary" onClick={()=>command("start")}><Play className="size-5"/>Start presentation</Button>:moment?<><Button size="lg" className="w-full" variant="primary" disabled={snapshot.status!=="presenting"} onClick={()=>command("open")}><Radio className="size-5"/>Open voting</Button><div className="grid grid-cols-2 gap-3"><Button size="lg" variant="secondary" disabled={snapshot.status!=="active"} onClick={()=>command("close")}><CircleStop className="size-5"/>Close</Button><Button size="lg" variant="secondary" disabled={snapshot.status!=="closed"} onClick={()=>command("reveal")}><Eye className="size-5"/>Reveal</Button></div></>:<Button size="lg" className="w-full" variant="primary" disabled={snapshot.currentIndex>=snapshot.totalItems-1} onClick={()=>command("next")}><ChevronRight className="size-5"/>Advance slide</Button>}</div>
 <div className="mt-8 border-t border-[var(--color-border)] pt-5"><div className="grid grid-cols-2 gap-3"><Button variant="ghost" onClick={()=>command(snapshot.joinLocked?"unlockJoin":"lockJoin")}>{snapshot.joinLocked?<><Unlock className="size-4"/>Unlock join</>:<><Lock className="size-4"/>Lock join</>}</Button><Button variant="ghost" onClick={()=>command("showJoin")}><RotateCcw className="size-4"/>Join screen</Button></div><Button className="mt-3 w-full" variant="destructive" onClick={()=>confirm("End the session?")&&command("end")}><CircleStop className="size-4"/>End session</Button></div></div></main>}
